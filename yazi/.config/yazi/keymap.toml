[mgr]
prepend_keymap = [
  { on = "<C-p>", run = ''' shell 'qlmanage -p "$@"' --confirm ''', desc = "Preview file with macoS system Quick Look" },
]

# [[mgr.prepend_keymap]]
# on = "<C-p>"
# run = "shell -- qlmanage -p %s"
# desc = "Preview file with macoS system Quick Look"
